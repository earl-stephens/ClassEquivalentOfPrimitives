package application;

public class App {
	public static void main(String[] args)  {
		//primitive types in java
		//int, float, double, char, byte, short, long, 
		//also boolean
		//the class equivalents are Integer, Float, Double,
		//Character, Byte, Short, Long, Boolean
		
		//to use the class equivalent, for example
		//called autoboxing
		Integer intValue = 7;
		//same as
		int intValue2 = 8;
		
		/* when using Integer intValue = 7, Java is creating
		 * an object.  So can't compare objects, same as with String;
		 * have to use intValue1.equals(intValue2)
		 */
		
		/* cant use == or .equals with float and double
		 * because of lack of precision
		 */
		
		
		
	}
}
